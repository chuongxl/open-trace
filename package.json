{
  "name": "open-trace",
  "version": "0.1.0",
  "description": "Local AI usage observatory — track token usage, agent traces, and memory per project",
  "type": "module",
  "main": "daemon/index.js",
  "scripts": {
    "start": "node daemon/index.js",
    "dev": "node --watch daemon/index.js",
    "test": "node --test tests/*.test.mjs",
    "test:watch": "node --watch --test tests/"
  },
  "dependencies": {
    "express": "^5.2.1",
    "better-sqlite3": "^9.4.3",
    "chokidar": "^5.0.0",
    "uuid": "^14.0.2",
    "dotenv": "^16.4.5",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
